services:
  loxone-bridge:
    build:
      context: .
    image: loxone-bridge:local
    container_name: loxone-bridge
    command: ["loxone-bridge"]
    env_file:
      - .env
    restart: unless-stopped
    init: true
    ports:
      - "${HTTP_PORT:-8080}:${HTTP_PORT:-8080}"
